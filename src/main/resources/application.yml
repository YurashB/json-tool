spring:
  # Database
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/jsontool
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: create # When you launch the application for the first time - switch "none" at "create"
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect


server:
  port: 8081

application:
  security:
    access-token-secret: "8AD7F3E537787AA2CA7DB2F57FF29"
    refresh-token-secret: "C7DD82D7E842BFA43B5DA2BEBD717"
